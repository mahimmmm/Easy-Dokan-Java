<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Use NoActionBar as the parent as we use MaterialToolbar everywhere. -->
    <style name="Theme.EASYDOKAN" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_blue_primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pink_accent</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- Special theme for the transparent splash screen -->
    <style name="Theme.EASYDOKAN.Transparent" parent="Theme.EASYDOKAN">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>
